apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  namespace: team1
spec:
  template:
    spec:
      containers:
        - name: nginx
          env:
            - name: CUSTOM_ENV_VARIABLE
              value: custom_env_value️
